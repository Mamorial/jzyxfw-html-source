define(['app'], function(app) {

	app.register.controller('test1Ctrl', ['$scope', '$modalInstance', function ($scope, $modalInstance) {
		$scope.mes = '我是测试文字';

		$scope.sure = function() {
			$modalInstance.close();
		}

		$scope.close = function() {
			$modalInstance.close();
		};
	}]);

});


